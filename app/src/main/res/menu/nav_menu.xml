<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/home_icon"
        android:title="Home"
        android:icon="@drawable/home_icon"
        app:showAsAction="always"
        />

    <item android:id="@+id/explore_icon"
        android:title="Explore"
        android:icon="@drawable/search_icon"
        app:showAsAction="always"
        />

    <item android:id="@+id/cart_icon"
        android:title="Cart"
        android:icon="@drawable/cart_icon"
        app:showAsAction="always"
        />

    <item android:id="@+id/favorite_icon"
        android:title="Favorite"
        android:icon="@drawable/favorite_icon"
        app:showAsAction="always"
        />

    <item android:id="@+id/profile_icon"
        android:title="Profile"
        android:icon="@drawable/profile_icon"
        app:showAsAction="always"
        />
</menu>